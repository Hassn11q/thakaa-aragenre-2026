% AraGenre 2026 — Thakaa system description.
% Based on the official ACL template (acl_latex.tex) from
%   https://github.com/acl-org/acl-style-files
% acl.sty and acl_natbib.bst are used UNMODIFIED (verified identical to upstream).
%
% COMPILE WITH XeLaTeX:  xelatex -> bibtex -> xelatex -> xelatex   (or: tectonic)
% XeLaTeX is required because the paper sets Arabic examples via fontspec/polyglossia.
% Consequently [T1]{fontenc} and [utf8]{inputenc} from acl_latex.tex are intentionally
% omitted: they are pdfLaTeX-only and conflict with fontspec. Page geometry, margins and
% font size are unchanged (A4, 595.28 x 841.89 pt, 11pt), as set by acl.sty.
\documentclass[11pt]{article}
\usepackage[final]{acl}   % non-anonymous: system description papers are not anonymous
\usepackage{times}
\usepackage{latexsym}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{microtype}
\usepackage{url}
\usepackage{xurl}   % allow long URLs to break; ACL style files are untouched
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc}
\hypersetup{pdftitle={Thakaa at AraGenre 2026: Definition-Guided LLM Judging with Full-Taxonomy
Agreement-Gated Correction for Hierarchical Arabic Genre Classification},
pdfauthor={Hassan Alqaeri, Meshal Alamr, Abdullah Aldahlawi},
pdfsubject={AraGenre 2026 Shared Task, ArabicNLP 2026},
pdfkeywords={Arabic genre classification, hierarchical text classification, unseen label spaces,
definition-guided prompting, LLM-as-a-judge, retrieval-augmented classification}}

% ---- Arabic support (XeLaTeX/LuaLaTeX) ----
\usepackage{fontspec}
% acl.sty loads {times}, but under XeLaTeX fontspec overrides it, so we set the
% Times-metric-compatible Nimbus Roman explicitly to keep ACL's required typeface.
\setmainfont{Nimbus Roman}
\setmonofont{Nimbus Mono PS}
\usepackage{polyglossia}
\setmainlanguage{english}
\setotherlanguage{arabic}
% pick any installed Arabic font (Amiri, Scheherazade New, Noto Naskh Arabic)
\newfontfamily\arabicfont[Script=Arabic,Scale=1.0]{Amiri}
\newcommand{\ar}[1]{\textarabic{#1}}
% ------------------------------------------

\title{Thakaa at AraGenre 2026: Definition-Guided LLM Judging with\\
Agreement-Gated Correction for Hierarchical Arabic Genre Classification}

\author{
  Hassan Alqaeri \quad Meshal Alamr \quad Abdullah Aldahlawi \\
  Thakaa Advanced Company for Information Technology, Riyadh, Saudi Arabia \\
  \texttt{\{h.alqaeril, m.alamr, aldahlawi\}@thakaait.net}
}

\begin{document}
\maketitle

\begin{abstract}
We report the first-place system in AraGenre~2026 (0.7352 Hierarchical Macro~F1, 18 teams). The
task assigns each of 27{,}972 hidden-test Arabic segments one of 6 broad and one of 74 specific
genres, given only as English prose definitions, with the specific-label inventory
\emph{disjoint} from training. Our pipeline updates no model parameters but is not free of
task-specific effort: the judge prompt carries 74 hand-written cues, and components were
selected on the public leaderboard. A family-restricted, definition-guided
LLM judge scores candidate genres setwise under a hierarchically constrained decoding scheme,
reaching 0.7139. That base led on Specific Macro~F1 but trailed on Broad Macro~F1, so we add a
correction stage: two instruction models re-predict the specific genre, each shown the
\emph{entire} 74-definition taxonomy, and a label is edited only where both agree against the
base. This lifts Broad Macro~F1 to 0.892, largely by reducing \emph{topic-trap} calls, where the subject a text discusses displaces the
genre it instantiates. In a post-evaluation audit of
instances the base labelled book descriptions, Religious calls fall $394\rightarrow233$ once the
prompt states the type-versus-topic convention, and to 119 once it also shows the full taxonomy.
Refinements and an attractor drain reach 0.7352.
\end{abstract}

\section{Introduction}
Genre is what a text \emph{does} rather than what it is about: the same subject appears in a
contract, a news report and a textbook, and only the first is useful to someone assembling a
legal corpus. Sorting documents this way is what makes an archive filterable and a corpus
assemblable by register, and the Arabic resources for it remain comparatively thin.
AraGenre~2026~\citep{elhaj-etal-2026-aragenre} frames the problem as one of generalisation:
systems receive English genre definitions and limited synthetic training data, but the
hidden benchmark contains noisier naturally occurring Arabic spanning Modern Standard
Arabic, Classical Arabic, and multiple dialects, with \emph{previously unseen
genre--definition combinations}. Because the 74 specific test genres are disjoint from
training, no specific label can be memorised from it, and the setup pushes systems toward
generalising over communicative function. We placed 1st of 18 teams (Table~\ref{tab:metrics}) on the official ranking metric,
Hierarchical Macro~F1: the mean of Broad and Specific Macro~F1.

Our contributions are:
\begin{enumerate}
\itemsep0em
\item A hierarchically constrained, definition-guided LLM judge that reaches 0.7139
Hierarchical Macro~F1 with no parameter updates.
\item An account of why one predictor fails the two tiers differently: broad accuracy is the
family-accuracy of the specific prediction, whereas the gap between it and Broad Macro~F1 is
consistent with uneven performance across broad classes, so correction must be tier-specific
(\S\ref{sec:system}).
\item An agreement-gated correction stage in which each verifier sees the full taxonomy, with a
three-arm ablation splitting the topic trap between an explicit type-vs-topic instruction and a
visible label to move the error onto.
\item An attractor-drain step targeting the macro-F1 failure mode of heavily predicting a few
generic classes; its only gold-scored estimate is on dev, where it was also tuned.
\item Empirical validation on the hidden test: 0.7352 Hierarchical Macro~F1, ranked 1st of 18
teams, with the major system configurations scored as their own submissions.
\end{enumerate}

\noindent The main challenges were a specific-label inventory disjoint from training, the topic
trap of \S\ref{sec:system}, and classes for which we found no reliable written cue (song
dialects, textbook levels).

\section{Related Work}
\paragraph{Arabic and hierarchy-aware HTC.} Arabic pretrained models~\citep{antoun2020arabert,antoun2021aragpt2}
underpin most classification work on the language, including hierarchical variants~\citep{aragpt2htc2025,arabichtc2026}, and a large
literature models the label hierarchy explicitly~\citep{zhu2023hitin,cai2023hpt,zeng2025cpm,wang2025hierclassifier,combining2025,htcsurvey2024}.
These closed-label supervised setups do not directly address AraGenre's unseen,
definition-specified label inventory; we instead
enforce the hierarchy structurally, forcing $\text{broad}=\text{parent}(\text{specific})$.

\paragraph{Zero-shot HTC, LLM judging, and consensus.} Prior systems reach an unseen label
space through prompt ensembles~\citep{xia2025hieps}, LLM-built
prototypes~\citep{zhang2024hierprompt} or rewritten taxonomies~\citep{golde2025refined}; we
leave the definitions unrewritten and vary how much of the taxonomy is visible, though the base
judge appends hand-written cues to each (\S\ref{sec:system}). Our two-stage design retrieves
candidate definitions~\citep{chen2024retrieval} and compares them with an
LLM~\citep{sun2023rankgpt,zheng2023judging}, with
Qwen3-Embedding~\citep{qwen3embedding2025} bridging Arabic and English; the chain-of-thought
pass follows reasoning-based HTC~\citep{jiang2025reasoning,consistencyreg2024}. Ensembles of
diverse predictors~\citep{dietterich2000ensemble} appear in LLM form as juries, blending,
mixtures of agents, debate and
self-consistency~\citep{verga2024poll,jiang2023llmblender,wang2024moa,du2023debate,wang2023selfconsistency},
which aggregate free-form text or resample one model; we instead require two models to emit the
\emph{same} label.

\section{Background}
Each instance is \texttt{\{id, text\}}; systems output one \texttt{broad\_genre} (of 6:
Informative, Creative, Interactive, Learning, Legal, Religious) and one
\texttt{specific\_genre} (of 74), using exact taxonomy label names, each with an English
definition. The released data are tiny and synthetic-like (train: 140 items / 7 specific genres;
dev: 110 / 6), and the specific-label inventories are \emph{100\% disjoint} across train, dev
and test, so no supervised label set transfers. The \emph{hidden test} has 27{,}972 naturally
occurring instances over 74 specific genres in 6 families: Informative (42), Learning (12),
Creative (7), Religious (6), Interactive (4) and Legal (3); Informative is dominated by 20 job
domains and 13 book-description topics, and Creative holds 5 song dialects. Missing predictions
are counted incorrect, and Informative dominates our predictions ($\sim$50\%).

\section{System}
\label{sec:system}

\begin{figure}[t]
\centering
\resizebox{0.83\columnwidth}{!}{%
\begin{tikzpicture}[
  font=\small,
  node distance=2.2mm,
  box/.style={draw,rounded corners,align=center,inner sep=3pt,minimum height=5.4mm,fill=blue!5},
  base/.style={box,fill=blue!8},
  corr/.style={box,fill=orange!12},
  io/.style={align=center,font=\small\itshape},
  res/.style={align=center,font=\small\bfseries},
  ar/.style={-{Latex[length=2mm]},thick},
]
% base pipeline
\node[io] (text) {Arabic text $\langle$id, text$\rangle$};
\node[base,below=of text] (embed) {Qwen3-Embedding-8B retriever};
\node[base,below=of embed] (judge) {Family-restricted \texttt{gemma-4-31b} judge};
\node[base,below=of judge] (rules) {Marginal correction + surface rules};
\node[res,below=of rules] (basep) {Base \;\textbf{0.7139}};
\draw[ar] (text)--(embed); \draw[ar] (embed)--(judge);
\draw[ar] (judge)--(rules); \draw[ar] (rules)--(basep);
% correction stages
\node[corr,below=7mm of basep] (cons) {Agreement-gated correction, full taxonomy};
\node[corr,below=of cons] (drain) {Attractor drain, then surface rules};
\node[res,below=of drain] (finalp) {Final \;\textbf{0.7352} \;(rank 1)};
\draw[ar] (basep)--(cons); \draw[ar] (cons)--(drain); \draw[ar] (drain)--(finalp);
\begin{scope}[on background layer]
  \node[draw,dashed,rounded corners,fit=(text)(embed)(judge)(rules)(basep),
        label={[anchor=south east]north east:{\footnotesize Base}}] {};
  \node[draw,dashed,rounded corners,fit=(cons)(drain)(finalp),
        label={[anchor=south east]north east:{\footnotesize Correction}}] {};
\end{scope}
\end{tikzpicture}}
\caption{System overview. A Qwen3 retriever gates a broad family and a \texttt{gemma-4-31b}
judge scores its siblings setwise, giving the base (0.7139); GPT-5.6 and Gemini-3.6, each shown
all 74 definitions, then change a label only where they agree, and an attractor drain follows
(0.7352, rank~1).}
\label{fig:arch}
\end{figure}

\subsection{Base: family-restricted definition-guided judge}
The base system turns the 74-way choice into a within-family one (Figure~\ref{fig:arch}):
retrieval proposes candidates and a judge scores only siblings of a gated broad genre. The
setwise judge is \texttt{google/gemma-4-31B-it}~\citep{gemma4_2026}, served through vLLM under
the local alias \texttt{gemma-4-31b}; API model strings are pinned in
Appendix~\ref{app:hparams}. Each text passes through four steps.
(i) \texttt{Qwen3-Embedding-8B}~\citep{qwen3embedding2025} encodes the Arabic text and the
English definitions, and proposes candidate specific genres by cosine similarity.
(ii) Decoding is hierarchically constrained, all siblings for small families and top-$k$
retrieved for large topical ones, so that $\text{broad}=\text{parent}(\text{specific})$ and
broad \emph{accuracy} equals the family-accuracy of the specific prediction. Broad Macro~F1
does not: it averages per-class F1, and the gap between the two is what the diagnosis below
exploits. (iii) The judge scores each candidate 0--100 against its definition in one setwise
call~\citep{sun2023rankgpt}, and we take the argmax after a light marginal correction
(Appendix~\ref{app:calib}). (iv) A few
high-precision surface rules override the judge only on near-unambiguous markers
(Qur'anic mushaf orthography $\rightarrow$ \texttt{quran}; isn\=ad
openings $\rightarrow$ \texttt{hadith}; hand-screened samples showed nothing visibly violating the
intended rule semantics, which is not a precision estimate; emoji/hashtag short posts $\rightarrow$
Interactive subtypes). Appended to each definition the judge also sees a contrastive
discriminator, one per test genre: a single Arabic line naming the signal that separates it from
its family siblings, written from the released English definitions and Arabic linguistic
knowledge without reference to labelled data (Appendix~\ref{app:hparams}). On the hidden test
this base pipeline scores 0.7139.

\paragraph{Diagnosis from the scoreboard.}
The leaderboard exposed the structure: against the system then ahead of us, our base led on
Specific Macro~F1 (0.5487 vs.\ 0.5339) but trailed on Broad Macro~F1 (0.8792 vs.\ 0.90), so the
whole deficit sat in the broad tier. Broad accuracy (0.9003) exceeded broad macro, indicating uneven
per-class performance; reading our own predictions showed cross-family leakage, with Bible
passages labelled poetry or encyclopedia entries and news labelled legal.

\subsection{Correction: full-taxonomy agreement-gated re-prediction}
We re-predict the specific genre with two frontier instruction models
(GPT-5.6~Luna~\citep{gpt56luna2026} and Gemini-3.6~Flash~\citep{gemini36flash2026}),
\emph{feeding each the entire 74-definition taxonomy} ($\approx$2.9K tokens) grouped by
broad genre, and asking for the single best specific label. The two-model design is motivated by
the ensemble argument that combining diverse predictors beats any single
one~\citep{dietterich2000ensemble}, in its LLM form~\citep{verga2024poll,wang2024moa}, but we
never submitted either verifier alone, so agreement here is a conservatism device and not
evidence of an ensemble effect. (Claude~Sonnet~5~\citep{sonnet5_2026} was run on the
diagnostic pool but excluded from the deployed correction on cost grounds.)

\paragraph{What suppresses the topic trap.} Given only the 6 broad definitions, all three
verification models classified by subject matter rather than by communicative function: a blurb
summarising an Islamic work \emph{performs} the function of a book description, so the taxonomy
files it under Informative while every broad-only verifier answers Religious. The full-taxonomy prompt also carries an explicit type-vs-topic instruction, so a
post-evaluation three-arm ablation over the same 1{,}403 instances separates the two (one model,
GPT-5.6, identical pool): 394 are called Religious with neither, 233 with the instruction alone
($-41\%$) and 119 with the full taxonomy ($-49\%$). The two are not interchangeable: the instruction
lowers Religious calls on the book descriptions but raises them on the rest of the pool, whereas
the full taxonomy lowers them on both while raising them on base-Religious instances, widening
the separation from 21 to 41 percentage points (Table~\ref{tab:arms}). Base-family agreement rises in five of the six families
(Appendix~\ref{app:trap}); Appendix~\ref{app:errors} gives a worked example.

\paragraph{Conservative application.} The first model runs over all 27{,}972 instances, the
second only over the 8{,}041 where the first disagrees with the base, and a correction is
applied only where both name the same label. Of the 1{,}178 proposed cross-family moves, 460
fall in seven author-screened directions and are applied; the other 718, such as
Informative$\rightarrow$Learning, are rejected (Appendix~\ref{app:subs}). Within-family refinements are applied wherever both models
agree.

\subsection{Attractor drain: redistributing to low-frequency siblings}
Specific Macro~F1 averages F1 over all 74 classes, so a class that is systematically
under-predicted has low recall and low class-level F1. This stage is partly transductive: reading our own hidden-test
predictions during the evaluation phase revealed five \emph{attractor} classes worded broadly
enough to absorb much of what the judge cannot place confidently, leaving their rarer siblings
almost unused, so
redistributing labels from an attractor to those siblings moves the mean far more than accuracy.
The list comes from that prediction distribution, not from held-out gold. The drain is deterministic: when the base label is an attractor class and a separate
chain-of-thought re-judging pass~\citep{wei2022cot} names a different sibling in the same family, the
sibling replaces it (broad never changes). The surface rules are re-applied afterwards as a
guard (Appendix~\ref{app:subs}). On the base alone it moves 554 instances
(Appendix~\ref{app:drain}); in the final pipeline the correction runs first, so it fires on 383.

\section{Experimental Setup}
\label{sec:setup}
\paragraph{Data splits.} We update no model parameters, and each configuration predicts
automatically once fixed, but the configurations were adapted between submissions from
leaderboard feedback and from reading test inputs and our own outputs. An input is \texttt{\{"id","text"\}} with Arabic text such as
\ar{مطلوب سائق باص 24 راكب في شركة في دبي جبل علي} (test id 24486,
\emph{ma\d{t}l\=ub s\=a'iq b\=a\d{s} 24 r\=akib f\=i sharika f\=i Dubayy Jabal `Al\=i}, ``bus
driver wanted, 24-seater, for a company in Dubai, Jebel Ali''); our submitted system pairs it with
\texttt{broad\_genre} = Informative and \texttt{specific\_genre} = \texttt{drivers\_and\_}
\texttt{delivery\_jobs}.
\paragraph{Preprocessing.} We apply no text normalisation; retriever inputs are capped at 512
tokens and verifier inputs at 1{,}500 characters. Normalisation erases two orthographic signals: heavy diacritisation marks 77\% of the texts we label
\texttt{quran} and 43\% of poetry against 1\% elsewhere, and emoji mark 28\% of Interactive texts
against under 1\% elsewhere (Appendix~\ref{app:subs}).
\paragraph{Tools and metrics.} Qwen3-Embedding-8B under \texttt{sentence-transformers}, vLLM for
the local judge, vendor SDKs for the verifiers; we report Macro F1, Weighted F1 and Accuracy at
both tiers (Appendix~\ref{app:hparams}).
\paragraph{Cost.} The correction makes 36{,}013 calls, about US\$50; the base needs no paid API
(Appendix~\ref{app:hparams}).

\section{Results}

\begin{table}[t]
\centering\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lcc}
\toprule
System & Split & Hier.\ F1 \\
\midrule
Official baseline (embedding sim.) & dev & 0.4658 \\
Our judge (Gemma-4-31B) & dev & 0.8794 \\
Our judge (GPT-5.6) & dev & 0.8943 \\
\quad Gemma-4-31B + correction & dev & 0.8557 \\
GPT-5.6 judge + attractor drain$^{\dagger}$ & dev & 0.9358 \\
\midrule
Genre-general judge, no retrieval & test & 0.6067 \\
\quad + embedding retrieval gate & test & 0.6812 \\
\quad + family-restricted judging & test & 0.6882 \\
\textbf{\; + judge prompt, correction, rules} & test & \textbf{0.7139} \\
\quad + agreement-gated correction (subset) & test & 0.7224 \\
\quad + cross-family moves, all instances & test & 0.7256 \\
\textbf{\; + within-family refinements, drain} & test & \textbf{0.7352} \\
\bottomrule
\end{tabular}
\caption{Main progression on both splits. Dev is near-degenerate (its 6 specific genres map
one-to-one onto 6 broad, so all three F1 metrics coincide), so the hidden test is our primary
evaluation. \emph{Subset} corrects only instances whose broad label the base judged least
secure. Rows are successive submissions, not single-component ablations. $^{\dagger}$applied to the GPT-5.6 judge, tuned on dev, so not held out.}
\label{tab:ablation}
\end{table}

\begin{table}[t]
\centering\footnotesize
\setlength{\tabcolsep}{2.6pt}
\begin{tabular}{lccccccc}
\toprule
System & Hier & SpM & SpW & SpA & BrM & BrW & BrA \\
\midrule
Base (ours) & .714 & .549 & .584 & .591 & .879 & .900 & .900 \\
\; + correction & .722 & .553 & .592 & .598 & .892 & .910 & .910 \\
\textbf{Final (rank 1)} & \textbf{.735} & \textbf{.573} & \textbf{.613} & \textbf{.619} & \textbf{.898} & \textbf{.914} & \textbf{.914} \\
\bottomrule
\end{tabular}
\caption{Official metrics for our three scored systems on the hidden test, rounded to three
decimals (Sp/Br = Specific/Broad; M/W/A = Macro~F1, Weighted~F1, Accuracy). Correcting the verification subset lifts the broad tier by $+.013$ Broad Macro~F1;
the final row folds in the cross-family moves, the within-family refinements and the drain,
together worth $+.020$ Specific Macro~F1.}
\label{tab:metrics}
\end{table}

\subsection{Development-set results and component transfer}
\label{sec:dev}
Re-running the pipeline on dev (110 examples, official gold) separates what transfers from what
does not. No class list is carried over, and the two dev classes the drain fires on are the two
most-predicted there, the same two under both judges, so no gold and no particular base is
needed to find them. On dev it moves five labels for $+0.042$ (95\% CI $[+0.010, +0.082]$),
but four are a single confusion and the component was tuned on these same items, so this is
descriptive, not held-out, evidence. The correction stage does not: dev has no \texttt{*\_book\_description} and no scripture, so the trap it targets has zero
instances, and both corrections that fire are wrong. Residual dev disagreements stem from a
convention clash between splits (Appendix~\ref{app:convention}).

\section{Analysis}
\label{sec:analysis}
Broad-only prompting put 157 book descriptions under Religious unanimously across all three
models, which the full taxonomy largely reversed; one model alone disagreed with the base on
$\sim$29\% of specifics. Every alternative correction we tried fell below the base
(Appendix~\ref{app:negative}).

\paragraph{Qualitative prediction audit.} Three patterns dominate: cross-family topic leaks,
heavy attractor prediction, and song-dialect classes where most texts we assign to them carry
no marker from our cue inventory (App.~\ref{app:errors}).

\section{Conclusion}
A definition-guided judge, an agreement-gated correction and an attractor drain placed 1st of
18. Components were selected on the leaderboard, so adaptation to this test distribution cannot
be separated from the mechanisms we describe.

\section*{Limitations}

\textbf{What is and is not a score.} Every hidden-test \emph{score} we report is an official
evaluation-phase submission (Appendix~\ref{app:subs}); we made none after the deadline. The
instruction-only ablation arm (\S\ref{sec:system}) was run after the phase closed, and the
descriptive tables in Appendices~\ref{app:errors}, \ref{app:trap}, \ref{app:drain}
and~\ref{app:subs} are local analyses of submitted outputs, not scored runs.
\textbf{Validation.} The hidden test has no released gold and dev is near-degenerate, so
components were selected on the public leaderboard and by manual reading; the reported
progression therefore doubles as the selection signal, which risks fitting the test
distribution. No hidden-test gain carries a confidence interval, so those gains are
leaderboard-measured rather than statistically certified; Appendix~\ref{app:subs} gives the
scale of a class-level effect. The one component testable against gold is the drain, where a
paired bootstrap on dev puts its gain above zero (\S\ref{sec:dev}). \textbf{Unmeasured
components.} We never ran the judge with and without the 74 discriminators and per-family cue
lists, so we cannot separate their contribution from the rest of the prompt.
\textbf{Partly transductive.} The marginal correction subtracts a per-genre mean computed over
the whole test set (Appendix~\ref{app:calib}), so the base cannot label a document in isolation,
and two further choices were derived by reading our own predictions over the blind inputs: the
five attractor classes (\S\ref{sec:system}) and the seven gated broad-move directions
(Appendix~\ref{app:subs}). No hidden label was inspected and no prediction was edited by hand,
but the method is not fully inductive and neither choice is validated on gold.
\textbf{An untested comparison.} We ran GPT-5.6 over the full test set but never submitted it as
a system, so the claim that a cheap base judge with conservative correction beats direct
full-taxonomy prediction is untested. \textbf{Cost and ceilings.} Correction cost scales with
test size, and our cue inventory has low coverage among texts we predict as song dialects, so we
lack reliable surface cues for those distinctions.

\section*{Ethics Statement}
This work uses only the publicly released AraGenre data and collects nothing new. In line with
the shared-task rules all predictions are produced automatically: no submitted label was
hand-edited and we did not reverse-engineer the hidden evaluation labels. We did read system
outputs and their texts to design the automatic rules, but that is inspection of our own
outputs, not annotation of the evaluation set, and no such judgement enters a submission except
through a deterministic rule.

The taxonomy spans religiously and culturally sensitive categories, which we treat descriptively
as text-type labels: a misclassification carries no judgement on the authenticity, orthodoxy or
quality of a text, and models trained on skewed corpora may under-serve lower-resourced
dialects. Two misuses would concern us. Dialect is not provenance: pointed at ordinary prose,
the classifier reads as a guess at where a writer is from, and we have no evidence it supports
that use. And because the labels separate religious from informative text, genre output could be
used to route or suppress material by register rather than by content. The system assigns text
types, it does not assess them, and a 0.57 Specific Macro~F1 is not a basis for an automated
decision about a document or its author without a person in the loop.

Our correction stage sends Arabic text to third-party hosted APIs; the inputs are already-public
benchmark data, but the same pipeline should not be applied to private text without review.
Reliance on hosted models whose cost and behaviour may change limits equitable reproducibility;
the base retriever-plus-open-judge pipeline is the self-hostable alternative.

\section*{Acknowledgments}
We thank the AraGenre~2026 organisers for designing and running the shared task, for the
released taxonomy and definitions, and for their prompt support throughout the evaluation
phase. We also thank the maintainers of
the open models and libraries our pipeline builds on.

\bibliography{aragenre}

\appendix

\section{Reproducibility}
\label{app:repro}
\textbf{Official submission.} The results we report are those of our final submission,
evaluated 1~August 2026, which scored 0.7352 Hierarchical Macro~F1 (Specific Macro 0.5725,
Weighted 0.6125, Accuracy 0.6189; Broad Macro 0.8979, Weighted 0.9142, Accuracy 0.9138) over
27{,}972 of 27{,}972 gold instances with no missing prediction, ranking 1st of the 18 teams in
the organisers' final standings. Every hidden-test \emph{metric score} we report for a system
configuration comes from an official submitted file rather than a local estimate; the counts and
agreement tables elsewhere are local analyses of those files; the full record is in
Appendix~\ref{app:subs}.
No parameters are updated at any stage; task-specific information enters only through the
hand-written discriminators, dev-tuned prompts and leaderboard-guided component selection.

\textbf{Data:} we use only the
official AraGenre inputs and definition files, and no external labelled data. The consensus
verifiers receive the 74 definitions verbatim. The judge does not: by default it appends a
one-line Arabic discriminator to each definition and prepends per-family cue lists, both
author-written from the released definitions and Arabic linguistic knowledge, and both naming
genres explicitly (\texttt{src/discriminators.py}, \texttt{src/judge.py}). The surface rules
name a further seven labels. No test label or annotation informs any of it, but the pipeline is
not label-agnostic and we do not claim it is.

\textbf{Retrieval coverage.} The hidden test has no gold, so we cannot report recall against
true labels. We can report how often retrieval surfaced the label the system itself ended up
choosing: the base label lies in the retrieved top-20 for 98.4\% of instances, and within the
top-6 actually scored in the two large families for 93.1\% of the instances the base assigns to
them. The final label lies in the top-20 for 97.2\%; the remaining 2.8\% are labels the
correction stage or the rules introduced from outside the retrieved set. These are coverage
figures for our own predictions, not accuracy.

\textbf{Provenance of the hand-built parts.} Nothing below was tuned on held-out gold. The 74
discriminators and per-family cue lists were author-written from the released definitions and
Arabic linguistic knowledge, fixed before the configuration that used them was scored and not
revised afterwards. The five attractor classes, the seven accepted broad-move directions and the
2{,}635-instance verification subset were chosen by reading our own predictions over the
released test inputs. The calibration $\alpha{=}0.5$ replaced an initial 0.75 that over-flattened
the 74-class space (Appendix~\ref{app:calib}), and the broad gate is an earlier scored submission
of ours reused verbatim.

\textbf{Models:} the base retriever is Qwen3-Embedding-8B (4-bit NF4
weights with fp16 compute, 4096-dim, cosine over L2-normalised vectors, sequence length
capped at 512 tokens); the judge is a Gemma-family model
(\texttt{gemma-4-31b}) served via vLLM with setwise 0--100 scoring; the consensus verifiers
are GPT-5.6~Luna and Gemini-3.6~Flash, each queried with the full-taxonomy prompt.

\textbf{Settings:} the judge decodes at temperature 0 with seed 42; the Gemini verifier is
called with \texttt{temperature=0} and the GPT verifier with \texttt{reasoning\_effort=low} and
no temperature set. We record the parameters we sent; whether a hosted model honours them is outside our control,
and we do not treat \texttt{temperature=0} as guaranteeing deterministic output.
The retriever caches the top 20 specific genres per text; the judge scores every sibling in the
four small families and the top 6 retrieved elsewhere, in a single setwise pass, with marginal
correction $\alpha{=}0.5$.

\textbf{Pipeline and code:} the code and cached model outputs described in this appendix will
be released publicly; paths below are relative to the repository root and name what that release
contains. Every stage is one file: retrieval
(\texttt{src/retrieve.py}), the family-restricted judge (\texttt{src/judge.py}), the surface
rules (\texttt{src/rules.py}), the full-context verifiers (\texttt{src/verify.py}), the
consensus and drain assembly (\texttt{src/assemble.py}), and the submission builder
(\texttt{src/submission.py}), which enforces
$\text{broad}{=}\text{parent}(\text{specific})$ and validates that all 27{,}972 IDs are
present with exact labels.

\textbf{Scope of reproducibility.} The final submission can be reconstructed exactly from the
cached artifacts, but the base pipeline cannot be regenerated end to end. Two inputs to the trap
experiment illustrate the gap: the 2{,}635-instance verification pool, selected during the
evaluation phase as the instances whose broad label we judged least secure, and the broad-only
prompt behind arm~A. Both the pool file and arm~A's cached outputs ship, so
\texttt{src/trap\_table.py} rebuilds the table offline, but neither selection can be re-derived
from the code.

\textbf{Two cached inputs.} The broad gate the judge restricts to is the scored 0.6812
system, an earlier retrieval-gated system we submitted and scored, reused verbatim rather
than regenerated by the released code; and the base
predictions additionally carry an Interactive-recall reassignment applied during the
evaluation phase by a separate automatic judge pass (\texttt{src/interactive\_judge.py}), which
is included but is not wired into \texttt{reproduce.sh}; re-running the judge over the
same candidate sets reproduces 93.5\% of the base specific labels and 97.1\% of the broad
labels. Both cached files are included, so the submitted predictions rebuild exactly
(27{,}972/27{,}972) from cached outputs, but the base stage is a cached input rather than
something the repository regenerates end to end.

\textbf{Caveats:} the judge endpoint's serving precision was not
recorded (Appendix~\ref{app:hparams}), so the base stage is not bit-reproducible even with a
GPU; the hosted API models are versioned by release date and are not guaranteed
bit-reproducible across dates; the consensus stage incurs API cost proportional to the number of verified
instances (on the order of tens of US dollars for this test set). The base pipeline runs on
a single CUDA GPU (embeddings) plus a remote vLLM endpoint (judge).


\section{Annotation-Convention Divergence Between Splits}
\label{app:convention}
Five of the seven residual dev errors are texts that analyse religion from the outside, e.g.
\ar{تشير بعض الدراسات إلى أن أساليب الوعظ تختلف بين المجتمعات}
(\emph{tush\=iru ba`\d{d}u al-dir\=as\=ati il\=a anna as\=al\=iba al-wa`\d{z}i takhtalifu bayna
al-mujtama`\=at}, ``some studies indicate that preaching styles differ between societies''). Our system labels these \texttt{Analytical
Reports} (Informative); the dev gold label is \texttt{Religious Commentary} (Religious).

The released test taxonomy points the other way:
\texttt{islamic\_book\_description} (``texts summarising Islamic or religious books'') is
Informative and our correction stage moves such predictions out of Religious, consistently with
the released test definition. The examples suggest different boundary choices across the
disjoint dev and test taxonomies, although the differing label inventories prevent a direct
annotation-consistency comparison. A system following the released test taxonomy is therefore at a disadvantage on these dev
examples, an observed tradeoff rather than a formal bound. It also supports our central claim: the convention is carried by the
definitions, which is why supplying the full taxonomy, rather than the broad labels alone,
changes model behaviour so sharply.
\section{Prediction Audit Examples}
\label{app:errors}
Three recurring prediction patterns appear in our audit. (i)~\emph{Cross-family topic leaks}: a description \emph{of} a
religious book (\ar{يهدف هذا الكتاب إلى قراءة سيرة الرسول}, \emph{yahdifu h\=adh\=a al-kit\=abu
il\=a qir\=a'ati s\=irati al-ras\=ul}, ``this book aims at a reading of the Prophet's
biography'', id 10041) is
\texttt{islamic\_book\_description} (Informative), which both our base and final systems label
that way, consistent with the released definition, but which all three verifiers call Religious
under the broad-only prompt; scripture leaks the other way
(\ar{هؤلاء امراء بني عيسو}, \emph{h\=a'ul\=a'i umar\=a'u ban\=i `\=Is\=u}, ``these are the
chiefs of the sons of Esau'', Genesis~36, id 10798), where the base says
\texttt{history\_encyclopedia} and consensus changes it to \texttt{bible}. (ii)~\emph{Heavy attractor prediction}: long prose absorbed into \texttt{egyptian\_song\_lyrics}.
(iii)~\emph{Low cue coverage}: for each of the five song-lyric classes, 87--99\% of the texts we
assign to it carry no marker from our cue inventory
(\texttt{src/dialect\_markers.py} holds the lists and rebuilds the table). This measures the
coverage of our inventory, not an intrinsic ceiling: it shows our system lacks reliable surface
cues for these distinctions, not that no written signal exists.

\section{Negative Results}
\label{app:negative}
A \emph{direct} six-way broad classifier scored 0.6206; in our submitted configurations this
fell below deriving broad from the specific prediction, though one prompt configuration cannot
settle the comparison in general. Scoring all 74 candidates without retrieval pruning fell to
0.6517. For correction, plain self-consistency (0.7134), entropic optimal transport
(0.7115), uniform-prior calibration (0.7124) and an unrestricted chain-of-thought pass (0.7131)
all regressed below the 0.7139 base, whereas full-context consensus improved it. Cross-lingual rerankers failed~\citep{btzsc2026}, regex rules added nothing the judge lacked, and
the off-the-shelf dialect classifiers we tried did not expose the Iraqi/Sudanese
distinction that NADI-style country-level tasks define~\citep{nadi2021}.

\section{Full-Context vs.\ Broad-Only Prompting}
\label{app:trap}
Both verifiers were run over the same verification pool ($n=2{,}635$) once with only the
6 broad definitions and once with all 74 specific definitions. That pool was selected during
the evaluation phase as the instances whose broad label we judged least secure, so it is an
enriched diagnostic sample: rates measured on it are not representative of all 27{,}972. Because the full-taxonomy prompt
also carries an explicit type-vs-topic instruction, we added a third arm, broad definitions only
\emph{plus} that instruction, to separate the two factors. On the 1{,}403 book-description
instances GPT-5.6 calls 394 of them Religious with neither, 233 with the instruction alone and
119 with the full taxonomy; \texttt{src/trap\_table.py} rebuilds these counts from cached
verifier outputs with no API access.

Table~\ref{tab:arms} splits the same three arms over the whole pool. Two things follow. First,
the reduction is not a prior shift: from A to C the verifier calls Religious \emph{more} often on
the instances the base already reads as Religious and less often on every other subset, so the
separation between the two widens monotonically. Second, the instruction and the taxonomy are not
interchangeable. The instruction pays off only on the book descriptions, where the competing label
(\texttt{*\_book\_description}) is the trap's target; on the rest of the pool it over-fires,
raising Religious calls by a quarter. Only the full-taxonomy arm lowers them on the book
descriptions and the rest of the pool while raising them on base-Religious instances, which is
what widens the separation. We read
this as the instruction supplying a convention the model cannot act on until the taxonomy shows it
a label that satisfies the convention. The caveat of Table~\ref{tab:trap} applies here too: the
row labels come from the base system, so these are agreement counts, not accuracy.

\begin{table}[h]
\centering\small
\begin{tabular}{lrrr}
\toprule
Religious calls & A & B & C \\
\midrule
book descriptions ($n{=}1{,}403$)  & 394 & 233 & \textbf{119} \\
rest of pool ($n{=}1{,}169$)       & 121 & 152 & \textbf{61} \\
base-Religious ($n{=}63$)          & 26 & 28 & \textbf{30} \\
\midrule
separation (pp)                    & 21 & 29 & \textbf{41} \\
\bottomrule
\end{tabular}
\caption{The three arms over the full verification pool (GPT-5.6). A: broad definitions only.
B: \mbox{+ type-vs-topic} instruction. C: \mbox{+ full 74-definition taxonomy}. The first two rows
should fall and the third should rise; only C does both. Separation is the Religious-call rate on
base-Religious instances minus the rate on the other 2{,}572. Rebuilt by
\texttt{src/trap\_table.py}.}
\label{tab:arms}
\end{table}

Table~\ref{tab:trap} reports how often GPT-5.6's broad verdict still agrees with the base
system's family, one model across the two original prompts. Broad-only prompting collapses on
families whose members are \emph{about} another family's subject matter (Learning,
Informative), which is exactly the topic trap; under the full taxonomy those calls largely
disappear. Requiring both
verifiers to agree, joint Religious assignments fall from 134 under broad-only prompting to 37
under the full taxonomy, a 72\% reduction. That comparison is restricted to the 548 book
descriptions for which the second verifier has a call under both prompts, since the deployed
full-taxonomy run queries it only on the instances where the first verifier disagrees with the
base; on all 1{,}403, the first verifier alone falls from 394 to 126. That 126 is the deployed
all-instance run scored on this subset, not arm~C, which is a separate pool-matched run of the
same prompt. The two were issued 26 minutes apart on 31~July~2026 with the same model string and
the same instances, so the seven-prediction gap is run-to-run variation in the hosted model
rather than a difference in configuration; we did not record the exact model build returned by
either call.
All three broad-only runs are included, and \texttt{src/broad\_only\_check.py} reproduces these counts
and the 157 above without API access.

\begin{table}[h]
\centering\small
\begin{tabular}{lrcc}
\toprule
Base family & $n$ & broad-only & full-taxonomy \\
\midrule
Informative & 1682 & 49\% & \textbf{78\%} \\
Learning    &  305 & 18\% & \textbf{74\%} \\
Interactive &  153 & 46\% & \textbf{72\%} \\
Legal       &  281 & 53\% & \textbf{56\%} \\
Creative    &  151 & 50\% & 48\% \\
Religious   &   63 & 41\% & \textbf{48\%} \\
\bottomrule
\end{tabular}
\caption{Share of instances where GPT-5.6's broad verdict matches the base family, by
prompt context. This measures \emph{agreement with the base system}, not accuracy: the hidden
test has no released gold, so we can show that full context changes verifier behaviour and
that the change is consistent with the taxonomy's own conventions, but we cannot certify it as
an accuracy gain. Dev cannot serve as the gold-anchored counterpart here: it contains no
book descriptions and no scripture, so the trap this table measures has no instances there
(\S\ref{sec:dev}).}
\label{tab:trap}
\end{table}

\section{Attractor Drain}
\label{app:drain}
Relative to the base, the final system changes 459 broad and 2{,}890 specific labels (10.3\% of
instances). The drain is deterministic: it fires when the base label is an attractor class and
the chain-of-thought pass names a different sibling inside the same broad family
(Table~\ref{tab:drain}). No score threshold or entropy criterion is used, and
broad is held fixed by construction.

\begin{table}[h]
\centering\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrr}
\toprule
Class & base & moved \\
\midrule
\texttt{literature\_fiction\_bk\_desc.} & 598 & 208 (35\%) \\
\texttt{egyptian\_song\_lyrics}          & 979 & 212 (22\%) \\
\texttt{msa\_poetry}                     & 608 &  52 (9\%) \\
\texttt{culture\_encyclopedia}           & 455 &  41 (9\%) \\
\texttt{history\_encyclopedia}           & 632 &  41 (6\%) \\
\bottomrule
\end{tabular}
\caption{Attractor classes and how many predictions the drain reassigns to within-family
siblings when applied to the base predictions alone. In the submitted pipeline consensus is
applied first and the drain fires on 383 of these instances.}
\label{tab:drain}
\end{table}

\section{Calibration}
\label{app:calib}
The correction subtracts a scaled per-genre mean from each candidate's score,
$s'(y) = s(y) - \alpha\,\bar{s}(y)$, where $\bar{s}(y)$ is the mean score genre $y$
received over the whole test set and $\alpha{=}0.5$. It damps genres the judge rates generously
everywhere, which is what leaves rare siblings seldom predicted. We started from $\alpha{=}0.75$, chosen on the
released training labels; it over-flattened once the label space grew from 7 to 74 classes.
The correction is applied before the argmax and never changes the broad family. Because
$\bar{s}(y)$ is a batch statistic, this stage is transductive: a document's label depends on the
set it is scored with. Two
alternatives we tried instead of it, uniform-prior calibration and entropic optimal transport,
both scored below the base (Appendix~\ref{app:negative}).

\section{Hyperparameters and Prompts}
\label{app:hparams}
Retriever: Qwen3-Embedding-8B loaded in 4-bit NF4 with double quantisation and fp16 compute (the configuration the
submitted run used); only the cosine ranking is consumed, and we did not measure the effect of
NF4 quantisation on retrieval rankings, 4096-dim, L2-normalised, \texttt{max\_seq\_length} 512, the top 20 specific genres
cached per text; the judge scores every sibling in the four small families (Creative 7,
Religious 6, Interactive 4, Legal 3) and the top 6 retrieved in-family candidates in the two
large ones.

\paragraph{Judge.} \texttt{gemma-4-31b} via vLLM, setwise 0--100 scoring in a single pass,
temperature 0, seed 42, 3 retries with JSON-regex fallback, marginal correction
$\alpha{=}0.5$. We did not record the serving precision of the judge endpoint; vLLM's default
for this checkpoint is bfloat16. The retriever precision is given above.

\paragraph{Consensus verifiers.} GPT-5.6~Luna (\texttt{reasoning\_effort=low}, $\leq$500 completion tokens) and
Gemini-3.6~Flash (called with \texttt{temperature=0}, $\leq$400 output tokens), each given the full 74-definition taxonomy
(2{,}884 tokens under \texttt{o200k\_base}, giving a mean prompt of 3{,}034 tokens and a maximum
of 5{,}185) in the system prompt, and the text truncated to 1500 characters. The judge prompt asks for a
JSON array of integer scores over the candidate definitions; the consensus prompt asks for a single exact
\texttt{specific\_genre} identifier and states that a factual description \emph{of} a book is a
\texttt{*\_book\_description}, not the book's subject genre.

\paragraph{Software.} \texttt{torch}, \texttt{transformers},
\texttt{sentence-transformers} and \texttt{bitsandbytes} for the retriever, vLLM for the local
judge, and the \texttt{openai} and \texttt{google-genai} SDKs for the verifiers; pinned
versions accompany the code.

\paragraph{Cost.} The \$50 in \S\ref{sec:setup} is the amount billed for the 36{,}013 calls at
July~2026 prices; we did not log per-request token usage, so we cannot restate it as a rate.

\paragraph{Throughput.} A consensus call takes 2.0--2.8\,s. At the released default of 20
concurrent workers the 8{,}041-instance Gemini pass took 19\,min of wall clock; the full GPT
pass over 27{,}972 instances was run at higher concurrency.

\section{Official Submission Record}
\label{app:subs}
Every hidden-test metric score we report for a system configuration comes from an official
submitted file (Table~\ref{tab:subs}), so the progression is traceable to scored submissions
rather than to local approximations. Counts such as the arm totals and the agreement tables are
local analyses of those files, not scored quantities.

\begin{table}[!ht]
\centering\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lr}
\toprule
System & Hier.\ F1 \\
\midrule
genre-general judge, no retrieval   & 0.6067 \\
\; + embedding retrieval gate       & 0.6812 \\
\; + family-restricted judging      & 0.6882 \\
direct six-way broad judge          & 0.6206 \\
unpruned 74-way scoring             & 0.6517 \\
\; + surface rules only             & 0.6881 \\
rule variant                        & 0.6891 \\
prompt-ensemble blend               & 0.6883 \\
Interactive-subtype judging         & 0.6942 \\
hybrid judge variant                & 0.7128 \\
\; base system                      & 0.7139 \\
uniform-prior calibration           & 0.7124 \\
self-consistency ($K{=}3$)          & 0.7134 \\
entropic optimal transport          & 0.7115 \\
chain-of-thought applied wholesale  & 0.7131 \\
\; + agreement-gated corr.\ (subset)& 0.7224 \\
\; + cross-family moves, all inst.\ & 0.7256 \\
\textbf{\; + within-family refs, drain} & \textbf{0.7352} \\
\bottomrule
\end{tabular}
\caption{Official submissions behind Table~\ref{tab:ablation} and Appendix~\ref{app:negative}.
The final system produced 27{,}972 predictions over 27{,}972 gold instances, none
missing. We used 19 of our 26 permitted submissions; the 18 scored on the test phase are all
listed here. Submissions were made sequentially over four days, so the progression is a record
of scored configurations rather than a single controlled sweep.}
\label{tab:subs}
\end{table}

\paragraph{The scale of a stage gain.} Every specific class carries the same $1/74$ weight in
Specific Macro~F1 regardless of how often it is predicted, so moving one class's F1 from 0 to 1
shifts Specific Macro~F1 by 0.0135 and Hierarchical Macro~F1 by 0.0068. Measured against that
unit the individual stages in Table~\ref{tab:ablation} are small: the correction on the subset
is worth about 1.25 class-level effects, extending the cross-family moves about 0.47, and the
within-family refinements with the drain about 1.41. The full base-to-final gain is about 3.1
and the margin over the second-placed system about 2.7. These are coarse yardsticks, not
significance tests.

\paragraph{Two readings this record supports.} The 0.6881 row isolates the surface rules on top
of the 0.6882 row: they rewrite 32 labels (23 \texttt{quran}, 9 \texttt{hadith}) and leave the
score at 0.6881 against 0.6882. The rules had an approximately score-neutral effect in this configuration, so the
0.6882$\rightarrow$0.7139 gain belongs to the judge and calibration changes, not to them.
They were kept for a different job: in the final pipeline they fire on 1{,}087 instances and
agree with the base label on every one, so their whole net effect is to restore 75 labels that
consensus or the drain had moved away (56 \texttt{youtube\_comments}, 19 \texttt{quran}). They
guard the correction stages rather than adding score.
The chain-of-thought row applies that pass to every instance and scores 0.7131,
below the 0.7139 base; the same pass restricted to attractor classes is what the drain uses.
Chain-of-thought helps here only where it is aimed.

\paragraph{Gated broad moves.} Of the 3{,}300 instances where both verifiers agree against the
base, 2{,}122 stay inside the base family and are applied as refinements. Of the 1{,}178 that
would cross families, 460 fall in an author-screened direction and are applied:
Informative$\rightarrow$Interactive 195, Legal$\rightarrow$Informative 80,
Creative$\rightarrow$Religious 58, Informative$\rightarrow$Religious 58,
Creative$\rightarrow$Informative 44, Interactive$\rightarrow$Religious 20,
Legal$\rightarrow$Interactive 5; the final file differs from the base on 459 broad labels, one
fewer, because the closing surface-rule pass restores one. The remaining 718 are rejected, the largest groups being
Informative$\rightarrow$Learning (219), Creative$\rightarrow$Interactive (122) and
Learning$\rightarrow$Informative (99).

\paragraph{Preserved rule defects.} Three regexes in \texttt{src/rules.py} do not match their
intent: the Qur'anic class also admits U+0671, the opinion class lets a bare variation selector
match, and one character class contains a literal \texttt{|}. They ran as written when the
submission was produced, so we keep them verbatim and document them in the code; correcting all
three changes 22 of the 27{,}972 final predictions.

\paragraph{Orthographic signals.} Share of each group carrying heavy diacritisation (a mark on
over 10\% of its Arabic characters) or at least one emoji, over the final predictions:
\texttt{quran} 77\%/0\% ($n{=}691$), poetry 43\%/0\% ($n{=}1{,}606$), \texttt{hadith} 1\%/1\%
($n{=}647$), book descriptions 1\%/0\% ($n{=}3{,}916$), the whole Interactive family 0\%/28\%
($n{=}1{,}978$; \texttt{twitter\_posts} 57\%, \texttt{youtube\_comments} 30\%,
\texttt{app\_reviews} 11\%, \texttt{book\_reviews} 0\%).
Both are properties of the text, so no gold is needed;
\texttt{src/orthographic\_signals.py} reproduces the table.

\end{document}
