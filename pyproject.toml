[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
# Pinned so linting gives the same result in any environment.
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]  # long lines are left to the formatter
