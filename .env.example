# Nothing in this repository reads a .env file. Export these in your shell instead:
#   export OPENAI_API_KEY=...  GEMINI_API_KEY=...
# They are needed only to re-run the consensus verifiers from scratch; the cached artifacts
# let you reproduce the final submission without them.
OPENAI_API_KEY=
GEMINI_API_KEY=
