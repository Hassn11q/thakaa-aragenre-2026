# Required only to re-run the consensus verifiers from scratch (step 4 in README).
# The cached artifacts in artifacts/ let you reproduce the final submission without these.
GEMINI_API_KEY=
OPENAI_API_KEY=

# OpenAI-compatible endpoint serving google/gemma-4-31B-it.
JUDGE_BASE_URL=http://localhost:8000/v1
JUDGE_API_KEY=local-vllm
JUDGE_MODEL=google/gemma-4-31B-it
